import { MAKE_DISPLAY } from "../../../../../Utils/utils";

const display = MAKE_DISPLAY("none", "none", "inline", "inline");

export const KEYBOARD_ICON_STYLE = {
  ml: 2,
  ...display,
};

export const styles = { KEYBOARD_ICON_STYLE };
